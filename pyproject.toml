[project]
name = "djangoproject1"
version = "0.1.0"
description = "Add your description here"
requires-python = ">=3.12"
dependencies = [
    "django>=6.1",
    "scikit-learn>=1.9.0",
    "torch>=2.13.0",
    "transformers>=5.16.1",
]

[dependency-groups]
dev = [
    "kagglehub>=1.0.2",
]
